# Security

Never expose secrets.

Never commit:

.env

.env.local

Never expose service role keys.

Always use environment variables.

Update .env.example if new variables are required.

Never modify .gitignore to include secrets.